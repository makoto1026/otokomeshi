- if controller_name != 'sessions'
  = link_to "ログインはこっちだ！", new_session_path(resource_name), class: 'sign_in'
  %br/
- if devise_mapping.registerable? && controller_name != 'registrations'
  = link_to "新規登録はこっちだ！", new_registration_path(resource_name), class: 'sign_up'
  %br/
